{
  "name": "playground",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "temp": "tsc --watch index.ts && nodemon index.js",
    "build": "tsc -p ./tsconfig.json",
    "start": "node dist/index.js",
    "tsc": "tsc -p ./tsconfig.json --watch",
    "watch": "nodemon dist/index.js",
    "nodemon": "nodemon"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "nodemon": "^3.0.2",
    "typescript": "^5.3.3"
  }
}
